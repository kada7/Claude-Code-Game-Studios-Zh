---
paths:
  - "src/networking/**"
---

# 网络代码规则

- 服务器对**所有**游戏玩法关键状态拥有**权威性** — 绝不信任客户端
- 所有网络消息必须进行版本控制，以确保向前/向后兼容性
- 客户端在本地预测，与服务器进行同步 — 实现错误预测的回滚
- 优雅处理断开连接、重新连接和主机迁移
- 对所有网络日志进行速率限制，防止日志泛滥
- 所有网络传输的值必须指定复制策略：可靠/不可靠、频率、插值
- 带宽预算：定义并跟踪每种消息类型的带宽使用情况
- 安全性：验证所有传入数据包的大小和字段范围